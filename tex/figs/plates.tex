% The three reproduced plates.
% Rebuilt from their source PDFs by tools/relayout_plates.py; see tex/figs/SELECTED.md for the
% crop boxes and tex/PERMISSIONS.md before posting.
%
% A plate is kept only where a photograph or a render carries something a drawing cannot: that
% these hands really are that much larger than a human one, and that a contact defect which is
% invisible at the scale of the grasp is unmistakable at the scale of the contact. Everything
% else that was reproduced has been redrawn in the paper's own style (figs/fig_transmission,
% fig_collision, fig_tactiledepth, fig_contactlabel) or dropped, because the prose already made
% the point. That took eleven visual styles to four and eleven open permissions to two.
%
% All three are single column. Two are single panel; hand_scale_to_human is a grid because the
% grid is the comparison. None is a `figure*`: a borrowed plate has not earned the page width.
%
% One command per figure. A section author places a plate by writing, e.g.,
%   \plateHandScale
% at the point in the section where the float should be anchored, once: a plate called twice
% prints twice, on two pages, under two numbers.
%
% Every plate credits its source inside its own caption, with \src. One carries \licensed as
% well, because its source is CC BY 4.0 and the licence asks for an attribution line.

% =============================================================== Sec. III, hands

\newcommand{\plateHandScale}{%
  \begin{figure}[t]
    \centering
    \includegraphics[width=\columnwidth]{figs/selected/hand_scale_to_human.png}
    \caption{Five research hands photographed to scale beside an adult human hand
      and a ruler. A count of actuated degrees of freedom says nothing about the
      size of the hand that carries them.\src{\cite{leap_hand_2023}}}
    \label{fig:hand-scale}
  \end{figure}}

% ================================== Sec. V, teleoperation and human-data sources

\newcommand{\plateRetargetArtifacts}{%
  \begin{figure}[t]
    \centering
    \includegraphics[width=0.86\columnwidth]{figs/selected/teleop_retarget_artifacts.png}
    \caption{What a retargeting error looks like at the contact. The pose on the
      left is plausible; magnified, the spectacle temple passes through the
      finger. Joint-angle error reports this as small.\src{\cite{toporetarget_2026}}}
    \label{fig:teleop-retarget-artifacts}
  \end{figure}}

% ========================================================== Sec. VI, two hands

\newcommand{\plateGraspPenetration}{%
  \begin{figure}[t]
    \centering
    \includegraphics[width=0.86\columnwidth]{figs/selected/bimanual_grasp_penetration.png}
    \caption{Two failures of synthesised two-hand grasps, each enlarged at the
      contact: (A) a finger inside the object, (C) a finger inside the other hand.
      Both look plausible whole~\cite{bimangrasp_2024}.}
    \label{fig:bimanual-penetration}
    \licensed[panels (A) and (C) of Fig.~9]{Fig.~9 of Shao and Xiao, arXiv:2411.15903}{CC BY 4.0}{https://creativecommons.org/licenses/by/4.0/}
  \end{figure}}
